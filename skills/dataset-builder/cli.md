# Dataset Builder — CLI output rules

- No widgets or bracket UI commands.
- Print dataset links as plain numbered URLs.
- Summarise each source in one line: name, format, size if known.
- Offer to download to workspace with `web_download_file` if the user confirms.
