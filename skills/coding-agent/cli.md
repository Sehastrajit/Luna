# Coding Agent — CLI output rules

- Output code in fenced code blocks with the language tag.
- No widgets or UI commands.
- Shell commands that need confirmation: print "Run? [y/n]" pattern before executing.
- Keep explanations brief — one sentence before and after the code block.
