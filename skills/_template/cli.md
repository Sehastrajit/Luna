# Your Skill Name — CLI / Terminal rules

<!-- This file is optional. Only create it if you want different behaviour in the terminal. -->
<!-- These rules are appended after SKILL.md — do not repeat the base instructions here. -->

- No widgets or special commands — the terminal cannot render them.
- Keep the full answer under 40 lines. Offer to show more if needed.
- Use plain dashes ( - ) for bullet points, plain numbers for steps.
- URLs should be on their own line so they are clickable in most terminals.
