# Desktop Agent — CLI output rules

- No widgets or UI commands.
- Describe screen actions in plain text before executing them.
- Shell commands require explicit confirmation — print the command and wait.
