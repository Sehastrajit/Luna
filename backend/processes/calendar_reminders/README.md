# Calendar Reminders Process

Owns calendar/task notifications:

- Upcoming event reminders.
- Overdue task reminders.
- Morning greeting notification.

Scheduling is registered in `process.py`. Job exports live in `jobs.py`; move implementation bodies there when changing behavior.
